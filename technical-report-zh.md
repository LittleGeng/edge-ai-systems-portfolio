# AI 系统与具身智能技术报告

这份报告记录了两个机器人学习系统项目从官方仿真评测、RTX 4090 优化到 Jetson AGX Orin 部署的完整过程。所有实验在 **2026-08-07** 前完成；8 月 8 日至 9 日只进行了文件同步、清点和哈希校验，没有新增实验结果。文中的实测值都可以通过 [结果与证据索引](docs/evidence-map.md) 找到对应文件。

## 1. 项目概览

两个项目分别侧重不同的工程问题：

1. **EdgeDiffusion：** 在官方 Push-T 仿真中评测 Diffusion Policy checkpoint，在固定 RTX 4090 上比较 eager、`torch.compile` 和 TensorRT，并在 Jetson AGX Orin 上部署 FP16 TensorRT U-Net 引擎。
2. **SmolVLA Edge：** 在官方 LIBERO Spatial 任务中评测 LoRA 策略，在物理 Orin 上使用合成观测测量推理性能，并进一步测试 `torch.compile`、输入分辨率、INT8 量化和动作 chunk 调度。

EdgeDiffusion 的主要成果是：同一 RTX 4090 上，100 步去噪 P50 从 **566.71 ms** 降至 **235.80 ms**，加速 **2.40x**。单次 Orin U-Net 调用达到 **3.37/4.41 ms P50/P95**，在 50 ms 目标下 **0/500** 次超时。但完整的 scheduler + U-Net 循环在 100 步时仍需 **557.81/568.67 ms**；降至 6 步后虽然达到 **32.43/36.28 ms**，对应 Push-T 高质量完成率却降为 0%。因此，实测设置中没有一个同时满足时延和任务质量要求。

SmolVLA 的主要结论是：当前 PyTorch 路径只能达到 **0.837 Hz**，在 30 Hz 目标下 **500/500** 次超时。`torch.compile` 能降低固定合成输入下的首动作时延，但仍远未达到实时控制要求，任务质量的置信区间也不足以证明非劣效。256x256 直接缩放、6000 步 384x384 微调和定向 TorchAO INT8 均未得到可以继续采用的结果。

## 2. 这组实验回答什么

| 问题 | 实验 | 结果适用范围 |
|---|---|---|
| 策略在官方仿真器中的表现如何？ | Push-T 与 LIBERO 任务评测 | 仿真成功率不等于物理机器人成功率。 |
| 不同后端能否降低同一工作负载的时延？ | 固定 RTX 4090 上的 eager、compile 与 TensorRT 对比 | 数值只适用于给定模型、输入形状、步数和节点。 |
| Orin 上的推理能否达到局部时序目标？ | U-Net、完整去噪循环与策略推理测量 | 每项结果都明确说明是否包含 scheduler；均不包含完整机器人 I/O。 |
| 更快的方案是否保留任务质量？ | 使用相同 episode 的 Push-T 与 LIBERO 对比 | 速度结果和任务质量结果分别测量，再结合判断是否继续采用。 |

这些结果不应外推到所有 VLA 模型、所有 GPU 或物理机器人系统。

## 3. 实验设置

- Diffusion Policy 仓库 revision：`5ba07ac6661db573af695b419a7947ecb704690f`。
- Diffusion Policy checkpoint SHA256：`f804e16575e261fa0b7e981da3f67741fc8517817734320d550e43a4182bf876`。
- 目标板：Jetson AGX Orin 64GB，L4T 36.5.0。
- EdgeDiffusion 基础质量评测：60 个官方 checkpoint Push-T episode，环境种子互不重复。
- EdgeDiffusion 步数对比：100、20、10、8、6 步分别使用 20 个匹配的 Push-T episode。
- SmolVLA compile 对比：官方 LIBERO Spatial 的 10 个任务，每个任务 5 个 episode；eager 与 compile 使用相同 episode ID、seed-43 策略和 50 个动作步。
- SmolVLA 分辨率对比：沿用相同的 50 个 episode，只将 `resize_imgs_with_padding` 从 512x512 改为 256x256。
- SmolVLA 384px 微调：rank 32、学习率 `5e-4`、seed 43、6000 个 LoRA 步骤；继续扩大训练的预设条件是成功率至少达到 15%。
- SmolVLA INT8：TorchAO v0.13.0，revision `e318546f9be8c6dd1340157cd14e5cbc6ffa1f65`；每个变体在物理 Orin 上预热 5 次、测量 20 次，使用合成输入；带 LoRA adapter 的 Q/V projection 不参与量化。
- SmolVLA 调度实验：在阈值 0.5 和 0.8 下进行配对 LIBERO 仿真，注入物理 Orin 的 P50/P95 时延范围；这不是物理机器人实验，也不是逐条原始时间戳回放。
- Orin 测量分别记录首动作和稳定动作 chunk，并保留原始时序 CSV 与遥测。
- 公开仓库不包含凭据、私有端点、计费数据、面试问答或个人机器路径。

### 3.1 环境版本

| 工作负载 | 硬件与系统 | 软件、模型与产物版本 |
|---|---|---|
| EdgeDiffusion 云端基准 | NVIDIA GeForce RTX 4090；CUDA 12.4 | PyTorch 2.5.1+cu124；Torch-TensorRT 2.5.0+cu124；TensorRT 10.3.0；Diffusers 0.11.1 |
| EdgeDiffusion 目标板 | Jetson AGX Orin 64GB；L4T 36.5.0 | TensorRT 10.11.0.33；PyTorch 2.8.0a0+5228986c39.nv25.06；engine `770a439e18d2...` |
| SmolVLA 目标板 | Jetson AGX Orin 64GB；合成观测 | LeRobot `8fff0fde7c79f23a93d845d1a50e985de01f8b8a`；base model `c83c3163b8ca9b7e67c509fffd9121e66cb96205`；policy `435185f15d31...` |
| Push-T 质量评测 | 官方仿真协议 `edge-evaluation-protocol-v1` | repository `5ba07ac6661db573af695b419a7947ecb704690f`；checkpoint `f804e16575e2...` |

## 4. EdgeDiffusion 实验结果

### 4.1 Push-T 任务质量

官方 checkpoint 在 60 个 episode 上的高质量完成率为 **95.0%**（57/60），平均得分为 **0.959**。另一次独立的 20-episode 复跑采用与参考窗口相同的种子，结果同为 **90.0%**。这里的 20-episode 复跑用于检查结果能否重现，不应与另一组 60-episode 汇总直接等同。

### 4.2 固定 RTX 4090 上的后端对比

三种后端运行相同模型、输入形状和 100 步去噪工作负载。每种设置预热 2 次，再测量 30 次；功耗每 0.1 秒采样一次。计时从条件张量构造完成后开始。

| 后端 | P50 / P95 | 按 P50 计算吞吐 | 增量峰值分配 | GPU 平均采样功耗 | 最大去噪误差 |
|---|---:|---:|---:|---:|---:|
| Eager | 566.71 / 570.36 ms | 1.765 /s | 20.37 MiB | 96.67 W | 0.000000 |
| `torch.compile` | 347.34 / 356.44 ms | 2.879 /s | 20.37 MiB | 109.62 W | 0.000066 |
| TensorRT | 235.80 / 237.93 ms | 4.241 /s | 0.01 MiB | 131.41 W | 0.000100 |

按同节点 P50 计算，TensorRT 加速为 **2.40x**。数值误差、Push-T 任务质量和时延分别测量，避免只因速度更快就忽略行为变化。

### 4.3 Orin 上的单次 U-Net 调用

FP16 引擎直接在物理 Orin 上构建。20 个正确性用例全部通过，最大绝对误差为 **0.011283**。单次 U-Net 调用的 wall time 为 **3.37 ms P50**、**4.41 ms P95**，吞吐为 **280.87 次/秒**，500 次测量均未超过 50 ms。

这里的数字只覆盖**单次 U-Net 调用**，不含 scheduler 重复执行、观测处理、仿真器、相机、执行器、网络和安全逻辑。现有遥测也不能确认一个不与其他电源轨重叠的整机总功率值，因此没有计算每个动作的总能耗。

### 4.4 去噪步数与任务质量

100 步作为质量参考，20、10、8、6 步分别使用同样的 20 个 Push-T 种子进行比较。预先设定的容许差为平均得分不超过 0.05、完成率不超过 5 个百分点。

| 去噪步数 | 控制器 P50 | 控制器 P95 | 平均得分 | 高质量完成率 | 是否保留任务质量 |
|---:|---:|---:|---:|---:|---|
| 100 | 1076.5 ms | 1120.3 ms | 0.922 | 90% | 是 |
| 20 | 217.6 ms | 236.2 ms | 0.119 | 0% | 否 |
| 10 | 88.4 ms | 96.8 ms | 0.093 | 0% | 否 |
| 8 | 80.4 ms | 90.5 ms | 0.067 | 0% | 否 |
| 6 | 65.2 ms | 81.4 ms | 0.104 | 0% | 否 |

减少步数确实显著降低了时延，但 20、10、8、6 步的高质量完成率都降为 **0%**。因此这些设置只说明速度与质量之间的取舍，不能作为部署方案。

### 4.5 Orin 上的完整去噪循环

将 TensorRT U-Net 放入 DDPM scheduler 后，每个设置预热 3 次，再测量 20 次：

| 去噪步数 | Orin 循环 P50 | Orin 循环 P95 | 超过 50 ms | 时延结果 | 结合任务质量后的结论 |
|---:|---:|---:|---:|---|---|
| 100 | 557.81 ms | 568.67 ms | 20/20 | 未达到目标 | 保留任务质量，但时延过高 |
| 20 | 110.80 ms | 121.96 ms | 20/20 | 未达到目标 | Push-T 质量损失过大 |
| 10 | 54.88 ms | 64.15 ms | 20/20 | 未达到目标 | Push-T 质量损失过大 |
| 8 | 43.68 ms | 50.01 ms | 1/20 | 未稳定达到目标 | Push-T 质量损失过大 |
| 6 | 32.43 ms | 36.28 ms | 0/20 | 达到目标 | Push-T 质量损失过大 |

6 步循环能满足 50 ms 时延要求，但对应的 Push-T 高质量完成率是 0%；100 步能保留质量，却每次都超过 50 ms。实测范围内没有同时满足两个条件的步数。该循环仍使用合成固定形状张量，不包含观测、仿真器和机器人 I/O。

## 5. SmolVLA Edge 实验结果

### 5.1 任务质量与数据范围

三个 LoRA 微调种子在 45 次主条件运行中取得 **13.3%** 成功率，在 105 次次要条件运行中取得 **18.1%**。次要任务没有参与模型选择，但包含在最终微调数据中，因此这里不把它称为零样本迁移。

| 条件 | 唯一 episode / 运行次数 | 各训练种子成功率 | 两阶段 bootstrap 95% CI |
|---|---:|---|---:|
| 主选择任务 | 15 / 45 | 42: 13.3%, 43: 6.7%, 44: 20.0% | [2.2%, 26.7%] |
| 次要条件 | 35 / 105 | 42: 8.6%, 43: 20.0%, 44: 25.7% | [7.6%, 29.5%] |

三份 8 月 4 日的原始数据记录均包含 **432 个选定 episode**、**52,970 帧**和 **377/377 个相同的 parquet 分片哈希**。文件、frame index、task index、时间戳和元数据计数都没有发现缺失或失败行。2026-08-10T08:19:32+08:00 的操作只是对同步后的归档进行 `verify-only` 校验，并未重跑实验。这项检查覆盖 parquet 行与分片身份，不覆盖解码后视频像素或任务成功率。

### 5.2 Orin 运行时与性能剖析

SmolVLA PyTorch 路径在 Orin 上使用合成观测。首动作 P50 为 **1190.17 ms**，稳定 chunk P50 为 **1191.74 ms**，控制率为 **0.837 Hz**，500 次测量全部超过 30 Hz 对应的时限。

在另一组固定输入对比中，eager 首动作 P50 为 **1287.54 ms**，`torch.compile(reduce-overhead)` 为 **1005.55 ms**，降低 **21.9%**；峰值 RAM 从 **7813 MB** 增至 **8912 MB**。尽管 compile 有明显改善，两种设置仍然都远慢于 30 Hz。

| 变体 | 首动作 P50 | 首动作 P95 | 频率 | 30 Hz 超时 | 峰值 RAM | 相对 eager 最大动作差 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| `baseline-fixed` | 1287.54 ms | 1322.35 ms | 0.773 Hz | 20/20 | 7813 MB | 0.0000 | 参考 |
| `amp-fixed` | 1242.39 ms | 1274.36 ms | 0.804 Hz | 20/20 | 7785 MB | 0.0225 | 改善较小，仍不能实时运行 |
| `resize256-fixed` | 996.67 ms | 1033.76 ms | 0.998 Hz | 20/20 | 7694 MB | 2.0072 | 后续任务评测显示质量损失过大 |
| `compile-reduce-overhead-fixed` | 1005.55 ms | 1025.40 ms | 0.996 Hz | 20/20 | 8912 MB | 0.0767 | 点估计在范围内，但 CI 不确定且仍不能实时运行 |

分阶段 CUDA event 剖析使用 20 次固定输入调用和 10 个去噪步骤。代表动作与基线完全一致。

| 阶段 | 单次推理平均耗时 | 占完整推理比例 | 嵌套关系 |
|---|---:|---:|---|
| Prefix embedding | 332.45 ms | 27.04% | 顶层 |
| Prefix KV forward | 96.90 ms | 7.88% | 顶层 |
| 去噪步骤总计 | 785.77 ms | 63.92% | 顶层 |
| Denoise VLM forward | 744.92 ms | 60.60% | 包含在去噪步骤总计中 |
| 未归因 | 14.16 ms | - | 剩余部分 |

Denoise VLM forward 占完整推理的 **60.60%**，prefix embedding 占 **27.04%**，下一步优化应优先分析这两个阶段的 kernel 和导出路径。由于 Denoise VLM forward 已包含在去噪步骤总计中，表中的嵌套行不能直接相加。

### 5.3 定向 TorchAO INT8

后训练量化覆盖 SmolVLA action expert 中 80 个 K/O/MLP 线性层，并排除带 LoRA 的 Q/V projection。两个方案的动作差都在预先设定的范围内，但首动作时延都没有达到至少降低 5% 的要求。

| 变体 | 首动作 P50/P95 | 稳定态 P50/P95 | 频率 | 峰值 RAM | 动作最大/平均绝对差 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| Eager 基线 | 1202.12/1245.03 ms | 1203.49/1236.71 ms | 0.829 Hz | 8250 MB | 0/0 | 参考 |
| INT8 weight-only | 1415.67/1457.57 ms | 1417.84/1445.21 ms | 0.705 Hz | 8242 MB | 0.02460/0.00524 | 比基线更慢 |
| INT8 dynamic | 5450.43/5498.47 ms | 5427.35/5481.92 ms | 0.184 Hz | 8265 MB | 0.02534/0.00737 | 比基线更慢 |

按首动作 P50，weight-only 比 eager **慢 17.8%**，dynamic INT8 **慢 353.4%**。这个结论只适用于当前模型分区、TorchAO revision、NVIDIA 25.06 iGPU 容器和合成输入，不能解释为 INT8 在 Orin 上普遍更慢。

### 5.4 Eager 与 compile 的任务质量

两种设置使用相同策略和相同的 50 个 episode：

- Eager：**10/50 = 20.0%**。
- `torch.compile(reduce-overhead)`：**11/50 = 22.0%**。
- 配对差值：**+2.0 个百分点**。
- Bootstrap 95% CI：**[-10.0%, +14.0%]**。
- 预先设定的初步筛选范围：绝对差值不超过 5 个百分点，点估计在范围内。

点估计看起来没有明显退化，但置信区间跨出了 +/-5 个百分点，因此无法证明统计非劣效或优越，绝对成功率本身也仍然较低。

### 5.5 直接缩放到 256x256

保持 seed-43 策略、LeRobot revision 和 episode ID 不变，只将 `resize_imgs_with_padding` 从 512x512 改为 256x256。成功率从 eager 512x512 的 **10/50 = 20.0%** 降至 **0/50 = 0.0%**，配对差值为 **-20.0 个百分点**，bootstrap 95% CI 为 **[-32.0%, -10.0%]**。

低分辨率在固定输入测试中更快，但当前 checkpoint 对这种分辨率变化不够稳健，因此不能直接采用 256x256。若要继续这条路线，需要重训练或蒸馏后再做相同的配对任务评测。

### 5.6 384x384 微调尝试

使用 rank 32、学习率 `5e-4`、seed 43，在 384x384 下训练 6000 个 LoRA 步骤。相同 50 个 LIBERO Spatial episode 上，候选达到 **5/50 = 10.0%**，而 eager 512x512 为 **10/50 = 20.0%**。配对差值为 **-10.0 个百分点**，bootstrap 95% CI 为 **[-24.0%, +2.0%]**。

继续扩大到三个种子和 20k 步的预设条件是至少达到 **15.0%** 成功率。实测只有 **10.0%**，因此没有继续投入更多算力。

### 5.7 动作 chunk 调度

50-action chunk scheduler 在物理 Orin 上使用合成观测回放 300 个 tick：

| 补充阈值 | Chunk P50/P95 | 新鲜动作 tick | Fallback/underflow tick | 过期 tick |
|---:|---:|---:|---:|---:|
| 0.5 | 1302.48/1354.78 ms | 68.7% | 94 | 100 |
| 0.8 | 1325.58/1362.75 ms | 86.7% | 40 | 201 |

提高补充阈值将新鲜动作比例从 **68.7%** 提高到 **86.7%**，fallback tick 从 **94** 降到 **40**，但过期 tick 从 **100** 增加到 **201**。缓冲只能在队列不足和动作过期之间做取舍，不能提高策略本身的产出速度。这是合成输入回放，不是物理机器人任务成功率。

另一次离线 30 Hz 回放得到 **8/300** 个新鲜动作 tick 和 **284** 个安全 fallback tick。该回放使用实测 Orin 时序记录，不是新的仿真实验。

### 5.8 注入 Orin 时延后的 LIBERO 结果

为了观察板端时延下异步调度对任务质量的影响，将物理 Orin 的 P50/P95 时延端点注入配对 LIBERO 闭环运行。两个阈值分别使用 25 个配对 episode。

| 成功阈值 | 同步成功率 | 异步成功率 | 异步减同步 | Fallback / 队列空 tick | 结论 |
|---:|---:|---:|---:|---:|---|
| 0.5 | 24% | 8% | -16% | 3618 | 质量下降超过预设范围 |
| 0.8 | 40% | 4% | -36% | 4028 | 质量下降超过预设范围 |

更高的补充阈值减少了 underflow，却增加了动作年龄；注入时延后的 LIBERO 运行仍然损失任务成功率。该实验使用 P50/P95 汇总端点，不是逐 chunk 原始时间戳的完整回放，也不是物理机器人实验。

## 6. 工程取舍

1. **速度和任务质量分开测量。** 更快的后端只有在数值与任务行为仍可接受时才值得继续使用。
2. **区分局部推理与完整控制。** U-Net 和策略推理时延不包含传感器、执行器、网络和安全循环的全部开销。
3. **保留没有成功的实验。** SmolVLA 未达到 30 Hz，低步数、低分辨率和 INT8 也出现明显问题；这些结果直接决定下一步做什么以及不做什么。
4. **不估算缺乏依据的能耗。** 没有可靠整机功率轨时，将每动作能耗保留为空值。
5. **复跑关键结果。** Orin 引擎重建和 Push-T 种子窗口复跑用于确认核心结果能够重现。
6. **根据中间结果及时停止。** 384x384 候选没有达到 15% 的继续条件，因此没有执行更昂贵的三种子、20k 步训练。
7. **不要把权重更小等同于时延更低。** 当前 INT8 方案减少了估算权重载荷和 CUDA 分配，但实际端到端动作时延反而上升。

对于 EdgeDiffusion，下一步应尝试经过蒸馏或重训练的低步数策略，并继续以 100 步结果作为质量参考。对于 SmolVLA，应先对 denoise VLM forward 和 prefix embedding 做 kernel 或导出级分析，再决定是否投入 TensorRT、ONNX 或量化感知训练。

## 7. 结果复现

在仓库根目录运行：

```bash
python3 reproduction/package/reproduce.py
```

该脚本会从已包含的结果文件重新计算汇总值，并检查时序回放、数据记录、哈希和完成状态。它不会重新执行模型推理，也不需要云端凭据或仍在运行的 AutoDL/Orin 资源。

还可以运行公开仓库检查：

```bash
python3 scripts/verify_repository.py .
```

当前检查覆盖 **36/36** 个已完成任务，没有部分完成任务，也没有无效的资源引用。所有 headline 数值、适用范围和源文件都列在 [结果与证据索引](docs/evidence-map.md)。

## 8. 总结

这个项目没有证明“VLA 已经能在 Orin 上以 30 Hz 运行”。它完成的是另一件更具体的事：用固定版本和明确实验条件评测机器人学习策略，在 RTX 4090 上比较优化后端，把模型部署到物理 Orin 上，并用任务质量、完整循环时延和失败实验判断哪些方案值得继续。

最终结果既包括 TensorRT 的 **2.40x** 加速和单次 U-Net 的毫秒级时延，也包括当前 SmolVLA 路径无法实时控制、低步数损失任务质量、直接缩放和 INT8 不适用等结论。这些取舍共同构成了项目的系统工程价值。
